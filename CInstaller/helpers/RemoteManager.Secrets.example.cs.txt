namespace CInstaller.helpers;

// Copy this file to RemoteManager.Secrets.cs (git-ignored) and fill in a real token.
// Use a fine-grained PAT scoped to "Public Repositories (read-only)" — nothing more.
public static partial class RemoteManager
{
    private const string GithubToken = "PUT_YOUR_TOKEN_HERE";
}
